import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0B1222);
  static const Color surface = Color(0xFF131D31);
  static const Color surfaceLight = Color(0xFF1B2A47);
  
  // Accents (Paleta unificada)
  static const Color primary = Colors.indigoAccent;
  static const Color secondary = Colors.cyanAccent;
  static const Color tertiary = Colors.tealAccent;
  static const Color info = Colors.blueAccent;
  static const Color error = Colors.redAccent;
  static const Color success = Colors.greenAccent;

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textMuted = Colors.white54;
}
